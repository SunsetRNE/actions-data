# actions-data

Actions 监控数据仓库（纯 JSON，无代码）。

- `actions-runs.json`：55 个公开仓库的 Actions 运行状态快照（由主站 actions-monitor workflow 每 ~60 秒扫描生成）
- `actions-history.json`：状态变化时间流（最近 300 条）

数据通过 GitHub Pages 提供：`https://sunsetrne.github.io/actions-data/actions-runs.json`
